## 5. 表格组件样式

我想针对我的表格组件做一次样式的优化。 可以参考element组件库中table的样式。/Users/wangbin/workspace/temp/element-plus-dev/packages/components/table

要求：
1. 本次修改组件仅修改src/table 的样式，只有必要时再修改table逻辑，增加一些样式class 蹬。
2. 表格的灰色默认设置成#fafafa，悬停样式设置成半透明灰色。
3. fixed的表格shadow，我希望在滚动到两端（比如固定右侧的表格，滚动到最右侧时，shadow需要隐藏）时不显示，切shadow 也设置成较浅的颜色。
4. 边框模式的实现，右边框的after样式，目前视觉上较粗。帮我分析一下如何修改。
5. .bin-table-cell 左右padding 12px

以上优化任务帮我进行规划，有问题提问我。

---

## 5. 表格组件优化

我想针对我的表格组件做一次优化和功能扩展，主要实现<树形表格> 功能。

要求：
1. 本次修改组件仅修改src/table 这个文件夹下的组件，尽可能在本目录下修改。如需要新增帮助文件，再增加。
2. 树形表格的实现。可参考 /Users/wangbin/workspace/temp/element-plus-dev/packages/components/table-v2 这个组件库中的表格组件实现。
3. 数据结构一致。然后实现的时候需要保持我当前组件风格，不要做太大改动。
4. 树形表格的实现，需要在文档中展示。增加一个demo，英文demo保持同步。
5. 表格的基础样式需要做一些调整，默认表格的行高设定为40px。small表格设置为36px，更紧凑。
6. 表格功能现在的滚动方案是使用的默认滚动，能否参考element-plus实现，使用我内置的b-scrollbar来进行优化。
7. 表格的滚动优化，可先对齐b-scrollbar和elemnt-plus中/Users/wangbin/workspace/temp/element-plus-dev/packages/components/scrollbar实现。确认一致后，再优化表格的滚动方案。

以上优化任务帮我进行规划，有问题提问我。

---
## 4. 组件库文档站点界面优化。

目前组件库的文档站点，使用的是vitepress 来进行构建的，除了代码模块是默认的，其他默认文档基本都是md渲染的。

但是整个站点我还是觉得需要优化一下样式，尤其是首页。然后说明页面。以及顶部导航栏还有侧边栏。目前已经编写了一部分样式来覆盖样式，但还是请你使用frontend skills 来帮我优化一下样式。文档主题色和组件库primary主色调一致。

文档文件在demo文件夹，本次修改内容只在此文件夹下进行修改。

---

## 3. 黑色模式支持

我想实现一个黑色模式的支持，目前组件库中没有实现。

请扫描我所有src中的组件。给我设计一个黑色模式的样式支持，可以参考我button组件中，我的样式设定。我原本计划黑色模式单独在每个组件中实现。可以使用ConfigProvider方式注入模式来实现。

请帮我设计一个黑色模式切换的方案，有问题提问我。

---

## 2. 国际化支持

当前组件中并没有实现国际化方案。本次优化我希望增加国际化支持，目前暂定只支持中/英文切换，默认中文。

本次优化支持国际化方案，请参考 /Users/wangbin/workspace/ai-work/ai-kb-app 这个工程下的 src/locales 文件夹中的国际化实现方案。

主要实现以下一些支持

1. 国际化方案依赖vue-i18n，请参考目标工程下的src/locales 方式，实现基础依赖引入和切换方式实现。
2. 可以使用ConfigProvider方式注入模式来实现。语言切换。
3. 部分组件不需要国际化，如icon，button等。这部分组件你来分析是否需要国际化。如果需要，再创建语言编码。
4. 一些比较明确的需要国际化的组件，日期选择框，时间选择框，等，因为有星期，目前应该是写死的一、二、三、日这样。
5. 组件库基础国际化修改完毕后。需要同时新增一个同样的demo国际化内容，demo国际化实现时，语言设定可以存储在localstorage中。
6. 国际化demo中，需要把国际化语言切换按钮，放到demo的右上角。

约束：

1. 文档demo中的组件介绍文字，本次先不进行翻译，默认还是中文介绍，本次只翻译header，左侧组件名，以及开始、快速上手、指南中的相关文字。

以上优化任务帮我进行规划，有问题提问我。

---

## 1. 分析当前组件库工程

分析当前组件库的现状，包括技术栈、项目结构、组件列表等，然后想整理优化一下工程文件组织。

当前库是一个构建vue3组件库的工程，完成后会打包部署到npm上。本次想先进行一些工程优化。

本轮优化主要做一下几点：

1. 首先更新一下package.json中所有的依赖库。
2. 基于最新的vite版本，调整优化我的打包构建方式。
3. 必要的地方增加ts校验，类型约束等。
4. 参考我最新的一个工程/Users/wangbin/workspace/ai-work/contract-review，我希望我使用oxlint，eslint.config.js editorconfig以及最新的prettierrc.json方式来优化我当前工程。
5. 其他你分析出来需要做优化的地方。如build中的构建脚本，我希望也都是用ts。

约束：

1. demo是我的文档预览工程目录，本次不需要动这个路径。
2. 除非必要，src目录中的组件，比如affix，我习惯一个组件的内容都在同一个目录。这种方式不要修改。
3. ai-docs是我的ai文档目录，本次也不需要修改。
4. 组件库中的实现尽可能逻辑不要修改，只做ts类型约束和规范写法。

以上优化任务帮我进行规划，有问题提问我。
